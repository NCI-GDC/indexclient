[tox]
envlist= py37

[testenv]
passenv =
    USE_PYTEST_INDEXD
    USE_RUNNING_PG

deps=
    -rdev-requirements.txt

install_command = python -m pip install --no-deps {opts} {packages}

commands=
    env
    pip install . --force --upgrade
    python -m pytest {posargs: -lvvvs tests}


[testenv:unit_test]
deps=
    -rdev-requirements.txt

install_command = python -m pip install --no-deps {opts} {packages}

commands=
    env
    pip install . --force --upgrade
    python -m pytest {posargs: -lvvvs tests/unit}

[testenv:xdist]
setenv =
    USE_PYTEST_INDEXD = true
;    MacOS requires the following environment variable to run test in parallel
    OBJC_DISABLE_INITIALIZE_FORK_SAFETY = YES

deps=
    -rdev-requirements.txt

install_command = python -m pip install --no-deps {opts} {packages}

commands=
    env
    pip install . --force --upgrade
    python -m pytest {posargs: -lvvvs tests/integration} -n auto
